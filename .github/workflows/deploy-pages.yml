name: Deploy frontend to GitHub Pages

on:
  push:
    branches:
      - main
      - '**'
  pull_request:
    types: [opened, synchronize, reopened, closed]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: gh-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy-main:
    if: (github.event_name == 'push' && github.ref_name == 'main') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ github.event.repository.homepage != '' && format('{0}/', github.event.repository.homepage) || format('https://{0}.github.io/{1}/', github.repository_owner, github.event.repository.name) }}
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Set base path from repository slug
        run: echo "BASE_PATH=/${GITHUB_REPOSITORY#*/}" >> "$GITHUB_ENV"

      - name: Build production app at {base}/
        run: npx ng build --configuration production --base-href "${BASE_PATH}/"

      - name: Build stage app at {base}/stage/
        run: npx ng build --configuration stage --base-href "${BASE_PATH}/stage/" --output-path dist/frontend-stage

      - name: Assemble deployment directory
        run: |
          rm -rf dist/pages-main
          mkdir -p dist/pages-main/stage
          cp -R dist/frontend/. dist/pages-main/
          cp -R dist/frontend-stage/. dist/pages-main/stage/

      - name: Deploy prod + stage to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: frontend/dist/pages-main
          keep_files: true

  build-and-deploy-pr:
    if: github.event_name == 'pull_request' && github.event.action != 'closed'
    runs-on: ubuntu-latest
    environment:
      name: pr-preview-${{ github.event.number }}
      url: ${{ github.event.repository.homepage != '' && format('{0}/pr/{1}/', github.event.repository.homepage, github.event.number) || format('https://{0}.github.io/{1}/pr/{2}/', github.repository_owner, github.event.repository.name, github.event.number) }}
    defaults:
      run:
        working-directory: frontend
    env:
      PR_NUMBER: ${{ github.event.number }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Set base path from repository slug
        run: echo "BASE_PATH=/${GITHUB_REPOSITORY#*/}" >> "$GITHUB_ENV"

      - name: Build stage app at {base}/pr/{pr-number}/
        run: npx ng build --configuration stage --base-href "${BASE_PATH}/pr/${PR_NUMBER}/"

      - name: Deploy PR build to gh-pages/pr/{pr-number}
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: frontend/dist/frontend
          destination_dir: pr/${{ github.event.number }}
          keep_files: true

  build-and-deploy-pr-on-push:
    if: github.event_name == 'push' && github.ref_name != 'main'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Find open PR for pushed branch
        id: find_pr
        uses: actions/github-script@v7
        with:
          script: |
            const branch = context.ref.replace('refs/heads/', '');
            const { owner, repo } = context.repo;
            const head = `${owner}:${branch}`;
            const { data } = await github.rest.pulls.list({
              owner,
              repo,
              state: 'open',
              head,
              per_page: 1,
            });
            const prNumber = data.length ? String(data[0].number) : '';
            core.setOutput('pr_number', prNumber);

      - name: Checkout
        if: steps.find_pr.outputs.pr_number != ''
        uses: actions/checkout@v4

      - name: Setup Node
        if: steps.find_pr.outputs.pr_number != ''
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        if: steps.find_pr.outputs.pr_number != ''
        run: npm install

      - name: Set base path from repository slug
        if: steps.find_pr.outputs.pr_number != ''
        run: echo "BASE_PATH=/${GITHUB_REPOSITORY#*/}" >> "$GITHUB_ENV"

      - name: Build stage app at {base}/pr/{pr-number}/
        if: steps.find_pr.outputs.pr_number != ''
        run: npx ng build --configuration stage --base-href "${BASE_PATH}/pr/${{ steps.find_pr.outputs.pr_number }}/"

      - name: Deploy PR build to gh-pages/pr/{pr-number}
        if: steps.find_pr.outputs.pr_number != ''
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: frontend/dist/frontend
          destination_dir: pr/${{ steps.find_pr.outputs.pr_number }}
          keep_files: true

  cleanup-pr-preview:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gh-pages branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Remove PR preview directory
        run: |
          rm -rf pr/${{ github.event.number }}
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No PR preview directory to remove."
            exit 0
          fi
          git commit -m "Remove PR preview for #${{ github.event.number }}"
          git push
